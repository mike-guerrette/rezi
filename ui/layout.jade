doctype html
html
    head
        meta(name="viewport" content="width=device-width, initial-scale=1")
        title= title
        base(href=baseHref || '/')
        each file in css
            link(rel="stylesheet" href=file)
    body(ng-app="rezi")
        block content

    each file in js
        script(src=file)