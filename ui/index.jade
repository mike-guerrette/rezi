extends layout

block content
    nav(class="navbar navbar-default")
        div(class="container" ng-controller="ApplicationController")
            div(class="navbar-header")
                a(class="navbar-brand" href="/") Rezi
            div(class="collapse navbar-collapse")
                ul(class="nav navbar-nav navbar-right")
                    li(class="dropdown" ng-show="user")
                        a(href="#" class="dropdown-toggle" data-toggle="dropdown" aria-expanded="false") {{user.username}}
                            span(class="caret")
                        ul(class="dropdown-menu" role="menu")
                            li
                                a(href="#" ng-click="logout()") Sign out


    div(class="container" ui-view)